<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.group3.vitamins.finance.infrastructure.taxinvoice.TaxInvoiceCommandMapper">

    <!--
        ⚠️ company_id 필터 없음(의도적) — uk_tax_invoice_approval_no가 회사 스코프 없이 테이블 전체에서
        유일해야 한다(국세청 승인번호는 전국 유일값). deleted_at IS NULL만 거른다 — cash_flow의
        findExistingDedupKeys와 동일한 기조(소프트 삭제된 건 재사용 가능하다고 봄). 실제 승인번호가
        삭제 후 재등록될 일은 거의 없어 이 필터로 인한 리스크는 낮다고 판단(2026-08-12).
    -->
    <select id="findExistingApprovalNos" resultType="java.lang.String">
        SELECT approval_no
        FROM tax_invoice
        WHERE deleted_at IS NULL
        AND approval_no IN
        <foreach collection="candidates" item="no" open="(" separator="," close=")">
            #{no}
        </foreach>
    </select>

    <!-- 호출부(FinanceCommandService)에서 이미 중복 제거된 행만 넘어온다. type은 요청 전체에 하나뿐이라 반복 바인딩. -->
    <insert id="insertAll">
        INSERT INTO tax_invoice
        (company_id, type, approval_no, issued_no, supplier_biz_no, buyer_biz_no, buyer_name,
        supply_amount, tax_amount, total_amount, item_name, ceo_name, sub_biz_no, memo, source_type)
        VALUES
        <foreach collection="rows" item="r" separator=",">
            (#{companyId}, #{type}, #{r.approvalNo}, #{r.issuedNo}, #{r.supplierBizNo}, #{r.buyerBizNo}, #{r.buyerName},
            #{r.supplyAmount}, #{r.taxAmount}, #{r.totalAmount}, #{r.itemName}, #{r.ceoName}, #{r.subBizNo}, #{r.memo}, 'CSV')
        </foreach>
    </insert>

    <!--
        settle_block_id IS NULL 조건을 UPDATE 문 자체에 걸어서 "확인 후 쓰기" 사이의 틈을 없앤다 —
        두 사용자가 같은 tax_invoice를 동시에 매칭 시도하면 한쪽만 성공해야 한다(cash_flow의
        updateCashFlowMatch와 동일한 이유).
    -->
    <update id="updateTaxInvoiceMatch">
        UPDATE tax_invoice
        SET settle_block_id = #{settleId}, linked_by = #{linkedBy}, linked_at = #{linkedAt}
        WHERE tax_id = #{taxId} AND settle_block_id IS NULL AND deleted_at IS NULL
    </update>

    <!--
        ⚠️ 실적값(actual_amount·actual_date)은 건드리지 않는다 (2026-08-13 규칙 확정) — 실제로 돈이
        움직인 기록은 입출금 소관이라, 세금계산서 매칭은 status 만 올린다. 세금계산서 금액을 실적으로
        넣으면 paidAmountRatio(= actual 합계 ÷ total)가 "실입금 진행률"이 아니게 된다.

        status = 'PENDING' 조건은 "이미 입출금이 붙은 블록의 PARTIAL/COMPLETED 를 WAITING 으로
        떨어뜨리지 않기" 위한 것이다 — 그 경우 0행이 정상이고, 호출부는 실패로 처리하지 않는다.
    -->
    <update id="markSettlementBlockWaiting">
        UPDATE settlement_block
        SET status = 'WAITING'
        WHERE settle_id = #{settleId} AND status = 'PENDING'
    </update>

    <update id="clearTaxInvoiceMatch">
        UPDATE tax_invoice
        SET settle_block_id = NULL, linked_by = NULL, linked_at = NULL
        WHERE tax_id = #{taxId}
    </update>

    <!--
        status = 'WAITING' 조건이 "입출금이 아직 붙어 있는지"를 대신 판정한다 — 입출금이 붙어 있으면
        블록은 PARTIAL/COMPLETED 이므로 0행이 되고 상태·실적값이 그대로 남는다. 별도 EXISTS 조회가 필요 없다.
    -->
    <update id="resetSettlementBlockFromWaiting">
        UPDATE settlement_block
        SET status = 'PENDING'
        WHERE settle_id = #{settleId} AND status = 'WAITING'
    </update>

    <update id="updateTaxInvoiceMemo">
        UPDATE tax_invoice SET memo = #{memo}, updated_at = NOW()
        WHERE tax_id = #{taxId} AND deleted_at IS NULL
    </update>

    <!--
        settle_block_id IS NULL 조건을 UPDATE 문 자체에 걸어서 "매칭 확인 후 삭제" 사이의 틈을 없앤다 —
        확인 직후 남이 매칭하면 0행이 되고, 호출부가 400으로 되돌린다(cash_flow의 softDeleteBatch와 동일).
    -->
    <update id="softDeleteBatch">
        UPDATE tax_invoice SET deleted_at = NOW()
        WHERE deleted_at IS NULL AND settle_block_id IS NULL
        AND tax_id IN
        <foreach collection="taxIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="updateExcludedBatch">
        UPDATE tax_invoice SET is_excluded = #{isExcluded}, updated_at = NOW()
        WHERE tax_id IN
        <foreach collection="taxIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
