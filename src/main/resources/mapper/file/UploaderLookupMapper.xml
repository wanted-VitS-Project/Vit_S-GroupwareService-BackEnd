<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.group3.vitamins.file.infrastructure.adapter.UploaderLookupMapper">

    <!-- 업로더 스냅샷 — 이름(NOT NULL)·부서명·직급명. auth findProfile 조인 패턴 재사용. -->
    <select id="findByUserId"
            resultType="com.group3.vitamins.file.infrastructure.adapter.UploaderRow">
        SELECT e.name  AS name,
               d.name  AS department,
               jp.name AS position
          FROM employee e
          LEFT JOIN department d    ON d.department_id = e.department_id
          LEFT JOIN job_position jp ON jp.job_position_id = e.job_position_id
         WHERE e.user_id = #{userId}
    </select>

</mapper>
