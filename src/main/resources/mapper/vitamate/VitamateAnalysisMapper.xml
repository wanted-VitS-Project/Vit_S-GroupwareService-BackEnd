<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.group3.vitamins.vitamate.analysis.infrastructure.persistence.mapper.VitamateAnalysisMapper">

    <select id="findAccessibleVitamateBlock"
            resultType="com.group3.vitamins.vitamate.analysis.infrastructure.persistence.row.VitamateBlockContextRow">
        SELECT
            b.block_id AS blockId,
            vb.vitamate_block_id AS vitamateBlockId,
            b.step_id AS stepId,
            s.project_id AS projectId
        FROM block b
                 JOIN vitamate_block vb
                      ON vb.vitamate_block_id = b.type_id
                          AND vb.block_id = b.block_id
                 JOIN step s
                      ON s.step_id = b.step_id
        WHERE b.block_id = #{blockId}
          AND b.type = 'AI'
          AND b.deleted_at IS NULL
          AND vb.deleted_at IS NULL
          AND (
            EXISTS (
                SELECT 1
                FROM step_permission sp
                WHERE sp.step_id = b.step_id
                  AND sp.user_id = #{userId}
                  AND sp.permission != 'NONE'
            )
                OR (
                NOT EXISTS (
                    SELECT 1
                    FROM step_permission sp
                    WHERE sp.step_id = b.step_id
                      AND sp.user_id = #{userId}
                )
                    AND EXISTS (
                    SELECT 1
                    FROM project_member pm
                    WHERE pm.project_id = s.project_id
                      AND pm.user_id = #{userId}
                      AND pm.permission != 'NONE'
                )
                )
            )
    </select>

    <select id="countCompletedFileVersionsInProject" resultType="int">
        SELECT COUNT(DISTINCT fv.file_version_id)
        FROM file_version fv
        JOIN `file` f
        ON f.file_id = fv.file_id
        WHERE f.project_id = #{projectId}
        AND f.deleted_at IS NULL
        AND fv.deleted_at IS NULL
        AND fv.upload_status = 'COMPLETED'
        AND fv.file_version_id IN
        <foreach collection="fileVersionIds" item="fileVersionId" open="(" separator="," close=")">
            #{fileVersionId}
        </foreach>
    </select>

    <select id="findAccessibleAnalysis"
            resultType="com.group3.vitamins.vitamate.analysis.infrastructure.persistence.row.VitamateAnalysisRow">
        SELECT
            va.vitamate_analysis_id AS analysisId,
            b.block_id AS blockId,
            va.review_type AS reviewType,
            va.review_category_codes AS reviewCategoryCodes,
            va.additional_instruction AS additionalInstruction,
            va.analysis_status AS analysisStatus,
            va.result AS result,
            va.error_message AS errorMessage,
            va.request_hash AS requestHash,
            va.created_at AS createdAt,
            va.completed_at AS completedAt
        FROM vitamate_analysis va
                 JOIN vitamate_block vb
                      ON vb.vitamate_block_id = va.vitamate_block_id
                 JOIN block b
                      ON b.block_id = vb.block_id
                          AND b.type_id = vb.vitamate_block_id
                 JOIN step s
                      ON s.step_id = b.step_id
        WHERE va.vitamate_analysis_id = #{analysisId}
          AND b.type = 'AI'
          AND va.deleted_at IS NULL
          AND vb.deleted_at IS NULL
          AND b.deleted_at IS NULL
          AND (
            EXISTS (
                SELECT 1
                FROM step_permission sp
                WHERE sp.step_id = b.step_id
                  AND sp.user_id = #{userId}
                  AND sp.permission != 'NONE'
            )
                OR (
                NOT EXISTS (
                    SELECT 1
                    FROM step_permission sp
                    WHERE sp.step_id = b.step_id
                      AND sp.user_id = #{userId}
                )
                    AND EXISTS (
                    SELECT 1
                    FROM project_member pm
                    WHERE pm.project_id = s.project_id
                      AND pm.user_id = #{userId}
                      AND pm.permission != 'NONE'
                )
                )
            )
    </select>

    <select id="findAnalysisDocuments"
            resultType="com.group3.vitamins.vitamate.analysis.infrastructure.persistence.row.VitamateAnalysisDocumentRow">
        SELECT
            vad.file_version_id AS fileVersionId,
            f.name AS fileName
        FROM vitamate_analysis_document vad
                 JOIN file_version fv
                      ON fv.file_version_id = vad.file_version_id
                 JOIN `file` f
                      ON f.file_id = fv.file_id
        WHERE vad.vitamate_analysis_id = #{analysisId}
          AND vad.deleted_at IS NULL
          AND fv.deleted_at IS NULL
          AND f.deleted_at IS NULL
        ORDER BY vad.created_at ASC, vad.vitamate_analysis_document_id ASC
    </select>

    <select id="findAnalysisCitations"
            resultType="com.group3.vitamins.vitamate.analysis.infrastructure.persistence.row.VitamateAnalysisCitationRow">
        SELECT
            vac.rank_order AS rankOrder,
            vad.file_version_id AS fileVersionId,
            vac.document_chunk_id AS documentChunkId,
            dc.page_number AS pageNumber,
            vac.excerpt AS excerpt
        FROM vitamate_analysis_citation vac
                 JOIN vitamate_analysis_document vad
                      ON vad.vitamate_analysis_document_id = vac.vitamate_analysis_document_id
                 JOIN document_chunk dc
                      ON dc.document_chunk_id = vac.document_chunk_id
        WHERE vac.vitamate_analysis_id = #{analysisId}
          AND vac.deleted_at IS NULL
          AND vad.deleted_at IS NULL
          AND dc.deleted_at IS NULL
        ORDER BY vac.rank_order ASC
    </select>

    <select id="findProcessingAnalysisJob"
            resultType="com.group3.vitamins.vitamate.analysis.infrastructure.persistence.row.VitamateAnalysisJobRow">
        SELECT
            va.vitamate_analysis_id AS analysisId,
            va.processing_attempt_id AS attemptId,
            va.review_type AS reviewType,
            va.review_category_codes AS reviewCategoryCodes,
            va.additional_instruction AS additionalInstruction,
            s.project_id AS projectId,
            b.block_id AS blockId
        FROM vitamate_analysis va
                 JOIN vitamate_block vb
                      ON vb.vitamate_block_id = va.vitamate_block_id
                 JOIN block b
                      ON b.block_id = vb.block_id
                          AND b.type_id = vb.vitamate_block_id
                 JOIN step s
                      ON s.step_id = b.step_id
        WHERE va.vitamate_analysis_id = #{analysisId}
          AND va.processing_attempt_id = #{attemptId}
          AND va.analysis_status = 'PROCESSING'
          AND va.lease_expires_at > NOW()
          AND b.type = 'AI'
          AND va.deleted_at IS NULL
          AND vb.deleted_at IS NULL
          AND b.deleted_at IS NULL
    </select>

    <select id="findAnalysisJobDocuments"
            resultType="com.group3.vitamins.vitamate.analysis.infrastructure.persistence.row.VitamateAnalysisJobDocumentRow">
        SELECT
            vad.file_version_id AS fileVersionId,
            f.name AS fileName
        FROM vitamate_analysis_document vad
                 JOIN file_version fv
                      ON fv.file_version_id = vad.file_version_id
                 JOIN `file` f
                      ON f.file_id = fv.file_id
        WHERE vad.vitamate_analysis_id = #{analysisId}
          AND vad.deleted_at IS NULL
          AND fv.deleted_at IS NULL
          AND f.deleted_at IS NULL
        ORDER BY vad.created_at ASC, vad.vitamate_analysis_document_id ASC
    </select>

    <select id="findAnalysisJobChunks"
            resultType="com.group3.vitamins.vitamate.analysis.infrastructure.persistence.row.VitamateAnalysisJobChunkRow">
        SELECT
            dc.file_version_id AS fileVersionId,
            dc.document_chunk_id AS documentChunkId,
            dc.chroma_id AS chromaId,
            dc.page_number AS pageNumber,
            dc.excerpt AS excerpt
        FROM vitamate_analysis_document vad
                 JOIN file_version fv
                      ON fv.file_version_id = vad.file_version_id
                 JOIN document_chunk dc
                      ON dc.file_version_id = fv.file_version_id
        WHERE vad.vitamate_analysis_id = #{analysisId}
          AND vad.deleted_at IS NULL
          AND fv.deleted_at IS NULL
          AND dc.deleted_at IS NULL
          AND dc.embedding_status = 'COMPLETED'
        ORDER BY vad.created_at ASC, vad.vitamate_analysis_document_id ASC, dc.chunk_index ASC
    </select>

    <select id="findBlockAnalysisHistories"
            resultType="com.group3.vitamins.vitamate.analysis.infrastructure.persistence.row.VitamateAnalysisHistoryRow">
        SELECT
            va.vitamate_analysis_id AS analysisId,
            va.review_type AS reviewType,
            va.review_category_codes AS reviewCategoryCodes,
            va.additional_instruction AS additionalInstruction,
            va.analysis_status AS analysisStatus,
            va.created_at AS createdAt,
            va.completed_at AS completedAt
        FROM vitamate_analysis va
        WHERE va.vitamate_block_id = #{vitamateBlockId}
          AND va.deleted_at IS NULL
        ORDER BY va.created_at DESC, va.vitamate_analysis_id DESC
        LIMIT #{limit}
    </select>

</mapper>
