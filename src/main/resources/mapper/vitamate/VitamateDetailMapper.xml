<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.group3.vitamins.vitamate.infrastructure.blockdetail.VitamateDetailMapper">

    <select id="findByVitamateBlockIds"
            resultType="com.group3.vitamins.vitamate.infrastructure.blockdetail.VitamateDetailRow">
        SELECT
            vitamate_block_id AS vitamateBlockId,
            welcome_message AS welcomeMessage
        FROM vitamate_block
        WHERE vitamate_block_id IN
        <foreach item="id" collection="vitamateBlockIds" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND deleted_at IS NULL
    </select>

</mapper>
