<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.group3.vitamins.project.block.infrastructure.adapter.BlockIssueStatQueryMapper">

    <select id="countByBlockIds"
            resultType="com.group3.vitamins.project.block.infrastructure.adapter.BlockIssueStatRow">
        SELECT ib.block_id AS blockId,
        COUNT(*) AS totalCount,
        COALESCE(SUM(CASE WHEN i.status = 'DONE' THEN 1 ELSE 0 END), 0) AS doneCount
        FROM issue_block ib
        JOIN issue i
        ON i.issue_id = ib.issue_id
        AND i.deleted_at IS NULL
        WHERE ib.block_id IN
        <foreach item="id" collection="blockIds" open="(" separator="," close=")">#{id}</foreach>
        GROUP BY ib.block_id
    </select>

</mapper>