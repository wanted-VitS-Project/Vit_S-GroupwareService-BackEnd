# 그룹웨어 서비스 (3조) — AI 협업 지침

**최종 업데이트**: 2026-08-16 (§1·§3 경로 정정 — 문서 지도는 `.ai/README.md` 정본으로 일원화, 유령 경로 제거 · 전체 분량 축소 / §3 상태 게이트 대상 정정 — 게이트는 `.ai/docs/domain/**` 에만 적용, `.ai/api/*.md` 사본은 게이트 없음(`.ai/API.md` §1 과 모순 해소))
**최종 업데이트**: 2026-08-14 (§5 정정 — "테스트 0개" 서술 폐기, 실제 약 186개 존재하며 CI 가 검증)
**최종 업데이트**: 2026-08-07 (§8-1 신설 — 코드 제공 형식 고정: 파일마다 코드 **앞**에 무엇/왜 한 줄)
**작성/관리**: 김동현 (DevOps)

> 이 파일이 **메인 규칙 파일**이다. 루트 `CLAUDE.md` 는 이 파일을 가리키는 얇은 포인터다 (Codex → `AGENTS.md`, Claude Code → `CLAUDE.md` → `AGENTS.md` — 그래서 둘 다 루트에 있어야 한다).
> ⚠️ **규칙을 고칠 땐 이 파일만 수정하라.** `CLAUDE.md` 는 건드리지 않는다.

---

## 1. 문서 위치

📍 **문서 지도는 [`.ai/README.md`](.ai/README.md) 가 정본이다.** 문서를 찾을 땐 거기서 찾고, 추가·이동·삭제하면 거기부터 고쳐라. 아래는 진입점만 추린 것.

**팀 공유 (커밋됨)** — `.ai/README.md`(지도 정본) · `.ai/API.md`·`.ai/ARCHITECTURE.md` · `.ai/CONVENTION.md`·`.ai/INFRA.md`·`.ai/PIPELINE.md` · `.ai/docs/README.md`→`.ai/docs/global/`(도메인 공통 11종) · `.ai/api/{도메인}.md`(프론트와의 계약) · `.ai/template/`(양식)

### 🔒 개인 로컬 (`.gitignore` — 커밋 금지)

| 경로 | 역할 |
|------|------|
| `.ai/local/STATE.md` | 지금 어디까지 했나·로드맵·백로그 |
| `.ai/local/WORKLOG.md` | 끝낸 작업·트러블슈팅·부수결정 |
| `.ai/local/INFRA-real.md` | IP·엔드포인트·계정ID 등 민감 정보 |
| `.ai/local/archive/` | 마일스톤 종료 시 STATE·WORKLOG 이관 |
| `.ai/docs/domain/**` | 도메인 구현 계약 — **정본이지만 git 비추적**, 담당자 로컬에만 있다 (§3) |

> `.ai/local/` 파일이 없으면 `.ai/template/` 의 대응 템플릿을 복사해 생성하라.
> 두 폴더는 사람마다 각자 갖는다. **절대 커밋하지 말 것.**

---

## 2. 세션 연속성

사용자가 이전 맥락을 요청하는 뉘앙스로 말하면 — "이전 내용 보고 와" / "지난 대화 이어서" / "전에 어디까지 했지" / "이어서 하자" / "연속성 유지해줘" 등 —
**반드시** `.ai/local/STATE.md` 를 먼저 읽고 **완료된 것 / 남은 것**을 간략히 요약한 뒤 이어서 진행하라.

---

## 3. 필수 참조 문서 — 읽지 않고 작업 금지

아래 작업 전 **반드시** 대응 문서를 먼저 읽어라. 경로는 `.ai/` 기준, `global/` = `.ai/docs/global/`.

| 문서 | 읽어야 하는 시점 |
|------|-----------------|
| **`docs/README.md`** (도메인 인덱스) | 도메인 설계 판단 · Entity·테이블·마이그레이션 — 해당 도메인 문서를 여기서 찾아라 |
| **`global/BLOCK.md`** | 블록이 걸린 **모든** 작업 |
| **`global/PERMISSION.md`** | 권한 검사 · 관리자 화면 ⚠️ **`MASTER` 는 최상위가 아니다** |
| **`global/DELETE.md`** · **`CLEANUP.md`** | 삭제 동작 · soft delete 종속 데이터 정리 |
| **`global/CONCURRENCY.md`** | 동시수정이 걸리는 수정 API (낙관락 단일 정책) |
| **`global/FLYWAY.md`** | Flyway SQL · RDS 마이그레이션 · seed 변경 |
| **`global/MYBATIS.md`** · **`PERSISTENCE.md`** | Mapper·XML · 조회 SQL 설계 · JPA/MyBatis 역할 판단 |
| **`API.md`** | Controller·Service·DTO·Repository·엔드포인트·에러코드 작성/변경 · "API 만들어줘" |
| **`ARCHITECTURE.md`** | 새 도메인 패키지 · 레이어·패키지 위치 판단 |
| **`CONVENTION.md`** | 브랜치·커밋·PR/이슈 |
| **`INFRA.md`** | 서버·리소스·배포 · Docker/compose · 인프라 스크립트 |
| **`PIPELINE.md`** | `.github/workflows/` · CI/CD 잡 · 시크릿 참조 |

### 🚨 API 는 프론트와의 계약이다

**프론트엔드는 이미 명세서를 보고 개발 중이다.** 명세와 다르게 구현하면 연동이 깨진다.

- API 작업 전 `.ai/API.md` 와 `.ai/api/{도메인}.md` 를 **반드시** 읽어라. 사본이 **없으면 코드를 쓰지 말고** 사용자에게 명세를 요청하라.
- 경로·메서드·필드명·타입·상태코드·에러코드를 **임의로 바꾸거나 추가하지 마라.**
- 명세가 틀렸거나 구현 불가로 보이면 **코드를 바꾸지 말고 사용자에게 보고하라.** 명세 변경은 프론트에 영향을 주는 팀 합의 사항이다.

#### ⛔ 상태 게이트 — `.ai/docs/domain/**` 에만 적용

**정본은 로컬 문서다 (2026-08-04 변경)** — API 계약은 `.ai/api/{도메인}.md`, 도메인 구현 계약은 `.ai/docs/domain/**`.
⚠️ 상태 게이트가 걸리는 곳은 **`.ai/docs/domain/**` 뿐이다.** `.ai/api/{도메인}.md` 사본에는 게이트가 없다 (`.ai/API.md` §1).

| 대상 | 착수 판단 |
|------|----------|
| **`.ai/docs/domain/**`** (도메인 구현 계약) | 항목마다 **명세상태 마킹을 먼저 확인**하라 — 아래 표 |
| **`.ai/api/{도메인}.md`** (프론트와의 계약 사본) | 게이트 없음. 위 **"API 는 프론트와의 계약이다"** 규칙 그대로 — **명세가 있으면 그게 계약이니 그대로 구현**하고, **없으면 코드를 쓰지 말고 사용자에게 명세를 요청**하라 |

⚠️ `.ai/docs/domain/**` 는 **git 비추적(gitignore)이라 담당자 로컬에만 있다.** 클론 직후엔 없다 — 없으면 **추측으로 시작하지 말고** 담당자/사용자에게 요청하라.

| 상태 (`.ai/docs/domain/**`) | 구현 |
|------|------|
| `📝 초안` | ⛔ **구현 금지.** 요구사항이 안 굳었다는 뜻 — 굳으면 문서를 `✅ 확정` 으로 바꾼 뒤 넘어가라 |
| `✅ 확정` 이상 | ✅ 구현 가능. 이탈 금지 규칙 전면 적용 |

> **착수 여부는 로컬 마킹만으로 판단한다.** 노션 등 외부 사본이 로컬보다 낡은 걸 발견하면 사용자에게 알려라 (방치하면 프론트가 구버전을 본다).
> **설계는 예외** — "도메인 계약 설계해줘" 요청엔 `.ai/docs/domain/**` 에 `📝 초안` 으로 자유롭게 써도 된다. 금지 규칙은 **구현** 에 적용된다.
> API 명세 설계("API 설계해줘 / 명세 짜줘")도 `.ai/api/{도메인}.md` 에 바로 쓰면 된다 — 다만 **적힌 순간 그게 계약**이므로, 이미 있는 명세를 고칠 땐 코드보다 md 를 먼저 바꾸고 **프론트에 공유**하라.

#### Swagger — API 구현 시 **함께** 단다 (상세 `.ai/API.md` §2)

- Controller `@Tag` · 메서드 `@Operation` + `@ApiResponses` · DTO 필드 `@Schema` — **필수**
- 내용은 **명세를 그대로 옮긴 것**이어야 한다. 없는 설명을 지어내지 마라.
- 명세의 **에러코드를 빠뜨리지 마라.** 프론트가 에러 분기를 못 짠다.
- `example` 에 실제 계정·토큰·개인정보 금지. ⚠️ PUBLIC 레포다.

**금지**: 문서에 없는 엔드포인트·필드·리소스 이름·시크릿 키를 **추측해서 지어내기**(없으면 "명세에 없음 — 확인 필요"라고 말하라) · 명세와 다른 경로·이름·구조 사용 · "관례상 이게 맞으니까"로 명세 이탈.

문서 내용이 변경되면 **해당 문서도 함께 업데이트**하라 (STATE 동기화 규칙과 동일).

---

## 4. 기록 규칙

### 4-1. STATE 동기화

변경마다 즉시 말고 **이슈 완료 시점 또는 대화 마무리 지점**에 모아서 `.ai/local/STATE.md` 에 반영하라 (토큰 절약).

- **대상**: 인프라·파이프라인 설계 변경 · 정책/컨벤션 결정 · 액션 아이템 완료(`[ ]` → `[x]`) · 마일스톤 진행
- "반영했습니다" 고지 없이 **조용히 처리**하고, 메시지 마지막에 어느 파일에 기록했는지 **한 줄만** 언급
- 기존 포맷·스타일 유지 · **`최종 업데이트` 줄은 최근 3건까지만** 유지 (그 이전 것은 삭제)

### 4-2. WORKLOG 기록

이슈·기능 단위 작업이 완료되면 **반드시** `.ai/local/WORKLOG.md` 에 최신순으로 추가하라.

- **시점**: "완료"·"끝났어"·"다음으로 가자" 등 완료 의사 표시 · PR 생성 직후 · 동작 확인 후
- **항목**: 날짜·이슈 번호·브랜치명·상태 / **변경 파일**(생성·수정·삭제 구분, 경로 정확히) / **주요 작업 내용** / **트러블슈팅**(문제+원인+해결) / **부수 결정**(작업 중 내린 판단·컨벤션)
- 진행 중 작업은 `🚧 진행 중` stub 만 먼저 만들어도 된다.

### 4-3. 백로그

"나중에 하자"·"백로그 등록해줘" 이거나 발견한 개선점이 현재 범위 밖이면 → `.ai/local/STATE.md` 의 `🔖 백로그` 에 등록 날짜와 함께 추가. 처리되면 `[ ]` → `[x]`.

### 4-4. 팀 문서로 승격

WORKLOG 내용이 **팀이 알아야 할 수준**이 되면 옮겨라 — 인프라 구성 → `.ai/INFRA.md` · 워크플로우 → `.ai/PIPELINE.md` · 컨벤션 결정 → `.ai/CONVENTION.md` · 운영 절차 → `.ai/runbook/`(이미 존재, 문서 추가) · 장애 기록 → `.ai/incident/`(아직 없음, 첫 건에서 신설).

승격 시 **민감 정보(IP·엔드포인트·계정ID·시크릿 값)는 반드시 제거**하고, `.ai/README.md` 지도도 함께 고쳐라.

---

## 5. 프로젝트 개요

- **VitaminS (Vit_S)** — 그룹웨어 서비스, 3조 · 조직 `wanted-VitS-Project` (⚠️ 전 레포 **PUBLIC**)
- `com.group3.vitamins` · `rootProject.name = vitamins` · **Java 17** · **Gradle 9.5.1** (`gradlew`)
- **Spring Boot 3.5.16** (dependency-management 1.1.7) — 2026-07-28 에 4.0.7 → 3.5.16 하향. **Boot 4 문서를 참고하지 말 것.**
- **의존성**: Web(MVC) · WebFlux(WebClient) · Data JPA · MyBatis · Flyway · Security · Mail · **Session(Redis)** · Lombok · MySQL Connector · DevTools
- **내 역할**: DevOps (인프라·CI/CD·빌드 표준화·문서 체계) · **마일스톤**: 파이널 모듈 — 초기 세팅
- **연관 레포**: `Vit_S-GroupwareService-BackEnd`(이 레포) · `-FrontEnd` · `-Python-Server`
  > ⚠️ **3개 전부 DevOps 담당(김동현)이 관리한다.** 인프라·CI/CD 설계는 3개를 함께 놓고 하고, 이 레포의 컨벤션·워크플로우를 나머지 2개에도 동일 적용하는 것이 기본이다.

### ⚠️ 조용히 깨지는 지점

| 항목 | 규칙 |
|---|---|
| 세션 저장소 | **Redis 단일.** `spring-session-jdbc` 를 **넣지 마라** — 2종이 공존하면 어느 쪽이 잡힐지 설정으로 못 정한다. `spring.session.store-type` 은 Boot 3 에서 **삭제된 프로퍼티**라 적어도 무시된다 (2026-08-04 확인) |
| 설정 파일 | 프로필 설정은 **`.yml` 만** (`.yaml` 금지). `.coderabbit.yaml` 만 예외 — CodeRabbit 이 `.yaml` 만 인식 |
| 테스트 | `src/test/` 약 186 개 클래스를 CI 가 검증한다. **새 기능·수정엔 테스트를 함께 추가하라.** 부분 실행 시 `jacocoTestReport` 집계 실패는 테스트 결과와 무관 — `-x jacocoTestReport` 우회 |

### 5-1. 명령어

`./gradlew build` (빌드) · `build -x test` (테스트 제외) · `test` (전체 테스트) · `test --tests "com.group3.vitamins.클래스명"` (단일) · `bootRun` (로컬 실행) · `clean build` (클린 빌드)

---

## 6. 보안 규칙 ⚠️ PUBLIC 레포

이 레포는 **누구나 볼 수 있다.** 커밋되는 파일에 아래를 절대 쓰지 마라.

**금지**: 퍼블릭/프라이빗 IP · RDS·Redis 엔드포인트 · 도메인 · AWS 계정 ID·ARN·액세스 키·키페어 이름 · DB 비밀번호·API 키·토큰 등 시크릿 **값** · `.pem`·`.key`·`.env`·`application-local.yml` 등 자격 파일

**권장**: 시크릿은 **키 이름만** 기록(`DB_PASSWORD`, `AWS_ACCESS_KEY_ID`) · `.ai/INFRA.md` 에는 구조·역할·포트만 쓰고 실제 값은 `{{EC2_HOST}}` 형태로 마스킹 · 실제 값은 `.ai/local/INFRA-real.md`(gitignore) · 런타임 주입은 GitHub Repo Secrets → 환경변수

**작업 중 민감 정보를 커밋하려는 흐름을 발견하면 즉시 중단하고 사용자에게 알려라.**

---

## 7. 양식

PR·Issue·커밋 메시지는 해당 양식 파일을 **먼저 읽고** 그 형식에 맞춰라 — PR → `.ai/template/PR.md` · Issue → `.ai/template/ISSUE.md` · 커밋 → `.ai/template/COMMIT.md`.

---

## 8. 응답 스타일 (기본)

> 팀 공용 기준선이다. 개인 취향(친근체·이모지 강도 등)은 각자 `~/.claude/CLAUDE.md` 에 추가하라.

- **한국어**로 답한다. **결론 먼저, 설명 나중에.**
- 비교·정리·매핑은 **표**를 쓴다.
- 한 응답에 **4단계 이상 묶지 않는다.** 여러 단계는 순서 표를 먼저 제시하고 하나씩 진행.
- 코드 제공 형식은 **§8-1** 을 따른다.
- 선택지는 **추천안을 먼저** 놓고 장단점을 나눈 뒤 사용자 결정을 명시적으로 요청한다.
- 에러 대응은 `핵심 진단 한 줄 → 원인 → 해결 → 다음 행동` 순서.
- 매 응답 마지막에 **다음 액션**을 명확히 안내한다.

---

## 8-1. 코드 제공 형식 ⭐

> 설명은 코드 **앞**에 둔다 — 뒤에 붙이면 이미 다 읽은 뒤라 늦다.
> **코드 안 주석은 함수 설명만** 남기고, 설계 근거·경고는 코드 밖으로 뺀다.

**파일 하나당** — 코드 앞에 아래 헤더를 박는다.

```
### `상대/경로/파일명.java`
**무엇**: 이 파일이 하는 일 (한 줄)
**왜**: 왜 지금 이걸 만들거나 고치는지 (한 줄)
```

- **경로는 항상 명시**해 파일 경계가 눈에 보이게 하라. **무엇/왜는 각 한 줄** (문단으로 늘어지면 안 읽는다).
- **삭제·원복도 왜를 쓴다.** "안 쓰니까" 가 아니라 **무엇이 대체하는지** 밝혀라.

**파일이 3개 이상일 때** — ① 맨 위에 **전체 그림**(`지금 → 바꾼 뒤` 한 줄씩 + `그룹/파일 수/하는 일` 표) ② **성격별로 묶는다**(삭제·원복·신규·교체) — 나열만 하면 왜 이만큼 건드리는지 안 보인다 ③ 그룹 머리에 **그 그룹 전체의 왜**를 한 줄.

**설명은 표로** — 설계가 갈렸던 지점, 집계·필터 기준, 삭제 사유는 산문 말고 표로.

**⚠️ 조용히 깨지는 함정은 코드 위에** — 컴파일도 되고 예외도 안 나는데 값만 틀어지는 지점(순서 의존 매핑, `LIMIT` 위치, 트랜잭션 flush 등)은 해당 블록 **바로 앞**에 `⚠️` 로 경고한다. 코드 뒤나 응답 맨 끝은 늦다.

**마지막에 검증** — `무엇을 어떤 순서로 확인할지`. 빌드 통과는 검증이 아니다. **틀렸을 때 눈에 안 보이는 것부터** 확인하게 하라.

---

## 9. 절대 금지

- ❌ **선제적 커밋·푸시** (`git commit`/`git push`) → **"커밋해"·"푸시해" 명시 요청 시에만** 실행. 요청 없으면 양식대로 **메시지 텍스트만 제공** (2026-08-03 완화)
- ❌ **선제적 PR 생성** (`gh pr create`) → 기본은 텍스트만. "PR 올려" 등 **명시 요청 시에만** 실행 가능
- ❌ 필수 참조 문서(§3)를 읽지 않고 해당 영역 작업 시작
- ❌ 문서에 없는 리소스 이름·엔드포인트를 추측해서 지어내기
- ❌ 민감 정보를 커밋되는 파일에 작성 (§6)
- ❌ `.ai/local/` · `.ai/docs/domain/` 을 git 에 추가
- ❌ `CLAUDE.md` 에 규칙 본문 작성 (포인터만 유지)
- ❌ STATE/WORKLOG 기록 빼먹기
- ❌ 코드만 던지고 설명 없이 끝내기
