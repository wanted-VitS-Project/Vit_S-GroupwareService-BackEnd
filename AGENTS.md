# 그룹웨어 서비스 (3조) — AI 협업 지침

**최종 업데이트**: 2026-08-04 (§1·§3 — `.ai/ARCHITECTURE.md` 신설 등록, 헥사고날 구조 확정)
**최종 업데이트**: 2026-08-04 (§3 상태 게이트 완화 — 노션 동기화 확인을 구현 게이트에서 제거, 로컬 `.ai/docs` 마킹이 정본)
**최종 업데이트**: 2026-08-03 (§9 완화 — 명시적 요청 시 커밋·푸시 실행 허용, PR·이슈는 계속 금지)
**작성/관리**: 김동현 (DevOps)

> 이 파일이 **메인 규칙 파일**이다. 루트의 `CLAUDE.md` 는 이 파일을 가리키는 얇은 포인터다.
> 두 파일은 각 AI 도구의 자동 로드를 위해 **루트에 위치해야 한다** (Codex → `AGENTS.md`, Claude Code → `CLAUDE.md` → `AGENTS.md`).
> ⚠️ **규칙을 고칠 땐 이 파일만 수정하라.** `CLAUDE.md` 는 건드리지 않는다.

---

## 1. 문서 위치

운영 문서는 모두 `.ai/` 폴더에 있다. 본 지침에서 아래 파일들을 언급하면 이 경로 기준으로 읽고 쓰라.

### 📦 팀 공유 (git 커밋됨)

| 문서 | 경로 | 역할 |
|------|------|------|
| 문서 인덱스 | `.ai/README.md` | 전체 문서 지도 |
| **제품 정의** | `.ai/PRODUCT.md` | 문제 정의 · 방향성 · 핵심 기능 · **범위 밖** |
| **도메인 모델** | `.ai/DOMAIN.md` | 스테이지·스텝·블록·이슈 정의 + 확정 결정 |
| **유스케이스** | `.ai/USECASE.md` | 공고→실적 전 과정 흐름 (UC-01~19) |
| **API 규칙** | `.ai/API.md` | 명세 이탈 방지 규칙 + 공통 컨벤션 |
| **아키텍처 컨벤션** | `.ai/ARCHITECTURE.md` | 헥사고날 계층 구조 · 패키지 네이밍 규칙 |
| **API 명세 사본** | `.ai/api/{도메인}.md` | 노션 명세의 작업용 스냅샷 (도메인별) |
| 협업 규칙 | `.ai/CONVENTION.md` | 브랜치·커밋·PR·이슈 규칙 |
| 인프라 인벤토리 | `.ai/INFRA.md` | 서버·리소스 구조 (실제 값은 마스킹) |
| CI/CD 명세 | `.ai/PIPELINE.md` | 워크플로우 구조·배포 흐름 |
| 양식 모음 | `.ai/template/` | PR·Issue·커밋·문서 템플릿 |

### 🔒 개인 로컬 (`.gitignore` 처리됨 — 커밋 금지)

| 문서 | 경로 | 역할 |
|------|------|------|
| 현재 상태 | `.ai/local/STATE.md` | 지금 어디까지 했나·로드맵·백로그 |
| 완료 기록 | `.ai/local/WORKLOG.md` | 끝낸 작업·트러블슈팅·부수결정 |
| 실제 인프라 값 | `.ai/local/INFRA-real.md` | IP·엔드포인트·계정ID 등 민감 정보 |
| 아카이브 | `.ai/local/archive/` | 마일스톤 종료 시 STATE·WORKLOG 이관 |

> `.ai/local/` 파일이 없으면 `.ai/template/` 의 대응 템플릿을 복사해 생성하라.
> 이 폴더는 사람마다 각자 갖는다. **절대 커밋하지 말 것.**

---

## 2. 세션 연속성

사용자가 세션 시작 시 아래와 같은 뉘앙스의 말을 하면, **반드시** `.ai/local/STATE.md` 를 읽고 현재 상태를 파악한 뒤 답변하라.

트리거 예시:
- "이전 내용 보고 와" / "지난 대화 이어서" / "전에 어디까지 했지"
- "이어서 하자" / "연속성 유지해줘" / "전 세션 내용 참고해"
- 그 외 이전 맥락을 요청하는 모든 표현

읽은 후에는 STATE 기준으로 **완료된 것 / 남은 것**을 간략히 요약하고 이어서 진행하라.

---

## 3. 필수 참조 문서 — 읽지 않고 작업 금지

아래 작업을 시작하기 전 **반드시** 대응 문서를 먼저 읽어라.

| 문서 | 읽어야 하는 시점 |
|------|-----------------|
| **`.ai/DOMAIN.md`** | Entity · 테이블 · 마이그레이션 작성/수정 · 도메인 설계 판단 · 스테이지/스텝/블록/이슈가 관련된 모든 작업 |
| **`.ai/API.md`** | Controller · Service · DTO · Repository 작성/수정 · 엔드포인트 추가/변경 · Request/Response 구조 변경 · 에러코드 추가/변경 · 사용자가 "API 만들어줘" 라고 할 때 |
| **`.ai/ARCHITECTURE.md`** | 새 도메인 패키지 생성 · Controller/Service/Repository 등 레이어 구조 판단 · 패키지 위치 결정 |
| **`.ai/CONVENTION.md`** | 브랜치 생성 · 커밋 메시지 작성 · PR/이슈 작성 · 브랜치 전략 판단 |
| **`.ai/INFRA.md`** | 서버·리소스·배포 대상 관련 작업 · Docker/compose 수정 · 인프라 스크립트 작성 |
| **`.ai/PIPELINE.md`** | `.github/workflows/` 수정 · CI/CD 잡 추가/변경 · 시크릿 참조 추가 |
| **`.ai/docs/global/FLYWAY.md`** | Flyway SQL 작성/수정 · RDS 마이그레이션 판단 · seed 데이터 추가/변경 |

### 🚨 API 는 프론트와의 계약이다

**프론트엔드는 이미 노션 명세서를 보고 개발 중이다.** 명세와 다르게 구현하면 연동이 깨진다.

- API 작업 전 `.ai/API.md` 와 `.ai/api/{도메인}.md` 를 **반드시** 읽어라.
- `.ai/api/{도메인}.md` 가 **없으면 코드를 작성하지 말고**, 노션 명세를 옮겨달라고 사용자에게 요청하라.
- 경로·메서드·필드명·타입·상태코드·에러코드를 **임의로 바꾸거나 추가하지 마라.**
- 명세가 틀렸거나 구현이 불가능해 보이면 **코드를 바꾸지 말고 사용자에게 보고하라.** 명세 변경은 프론트에 영향을 주는 팀 합의 사항이다.

#### ⛔ 상태 게이트 — 구현 전 반드시 확인

**로컬 `.ai/docs/domain/**` 문서가 정본이다 (2026-08-04 변경).** 노션 반영 여부는 더 이상 구현 게이트가 아니다 —
로컬이 항상 최신이고, 노션 동기화는 사용자가 별도로 관리한다. 엔드포인트마다 로컬 문서의 **명세상태 마킹을 먼저 확인하라.**

| 상태 | 구현 |
|------|------|
| `📝 초안` | ⛔ **구현 금지.** 팀 결정·요구사항이 아직 안 굳었다는 뜻 — 굳으면 로컬 문서를 `✅ 확정` 으로 바꾼 뒤 넘어가라 |
| `✅ 확정` 이상 | ✅ 구현 가능. 이탈 금지 규칙 전면 적용 |

> ⚠️ 노션 페이지가 로컬보다 낡은 경우가 있다(발견 시 사용자에게 알려라 — 팀 계약 문서라 방치하면 프론트가 구버전을 본다).
> 하지만 **구현 착수 여부는 로컬 마킹만으로 판단한다.**

> **설계는 예외다.** "API 설계해줘 / 명세 짜줘" 요청에는 `.ai/api/{도메인}.md` 에 `📝 초안` 으로
> 자유롭게 작성해도 된다. 위 금지 규칙은 **구현** 에 적용된다.

#### Swagger 어노테이션

API 를 구현할 때 Swagger 어노테이션을 **함께** 단다. 상세 규칙은 `.ai/API.md` §2.

- Controller 에 `@Tag`, 메서드에 `@Operation` + `@ApiResponses`, DTO 필드에 `@Schema` — **필수**
- 어노테이션 내용은 **명세를 그대로 옮긴 것**이어야 한다. 없는 설명을 지어내지 마라.
- 명세에 있는 **에러코드를 빠뜨리지 마라.** 프론트가 에러 분기를 못 짠다.
- `example` 에 실제 계정·토큰·개인정보를 넣지 마라. ⚠️ PUBLIC 레포다.

**금지 사항**:
- 문서에 없는 엔드포인트·필드·리소스 이름·시크릿 키를 **추측해서 지어내지 마라.** 없으면 "명세에 없음 — 확인 필요"라고 말하라.
- 명세와 다른 경로·이름·구조를 사용하지 마라.
- "일반적인 관례상 이게 맞으니까" 로 명세를 벗어나지 마라.

문서 내용이 변경되면 **해당 문서도 함께 업데이트**하라 (STATE 동기화 규칙과 동일).

---

## 4. 기록 규칙

### 4-1. STATE 동기화

토큰 소비를 줄이기 위해 변경마다 즉시 반영하지 말고, **이슈 하나 완료 시점 또는 대화 흐름상 마무리 지점**에 모아서 반영하라.

반영 대상:
- 인프라·파이프라인 설계 변경
- 정책/컨벤션 결정
- 액션 아이템 완료 처리 (`[ ]` → `[x]`)
- 마일스톤 진행 상황

반영 시 지킬 것:
- 사용자에게 "반영했습니다" 별도 고지 없이 **조용히 처리**하고, 메시지 마지막에 어느 파일에 기록했는지 한 줄만 언급
- 기존 포맷과 스타일 유지
- **`최종 업데이트` 줄은 최근 3건까지만 유지**하고 그 이전 것은 삭제하라 (무한 누적 방지)

### 4-2. WORKLOG 기록

이슈 또는 기능 단위 작업이 완료되면 **반드시** `.ai/local/WORKLOG.md` 에 최신순으로 추가하라.

기록 시점:
- 사용자가 "완료", "끝났어", "다음으로 가자" 등 완료 의사를 표시할 때
- PR 생성 직후
- 동작 확인 후

기록 항목:
- 날짜, 이슈 번호, 브랜치명, 상태
- **변경 파일** (생성/수정/삭제 구분, 경로 정확하게)
- **주요 작업 내용**
- **트러블슈팅** (문제 + 원인 + 해결) — 있다면
- **부수 결정** (작업 중 내린 판단·컨벤션 결정) — 있다면

진행 중 작업은 `🚧 진행 중` 상태로 stub만 먼저 만들어도 된다.

### 4-3. 백로그

사용자가 "나중에 하자", "백로그 등록해줘" 라고 하거나, 발견한 개선점이 현재 작업 범위를 벗어나면
→ `.ai/local/STATE.md` 의 `🔖 백로그` 섹션에 등록 날짜와 함께 추가. 처리되면 `[ ]` → `[x]`.

### 4-4. 팀 문서로 승격

개인 WORKLOG에 기록한 내용 중 **팀이 알아야 할 수준**이 되면 팀 공유 문서로 승격하라.

| 개인 기록 | 승격 대상 |
|---|---|
| 인프라 구성 변경 | `.ai/INFRA.md` |
| 워크플로우 구조 변경 | `.ai/PIPELINE.md` |
| 컨벤션 결정 | `.ai/CONVENTION.md` |
| 장애 / 운영 절차 | `.ai/` 에 `incident/` · `runbook/` 폴더 신설 후 이관 |

승격 시 **민감 정보(IP·엔드포인트·계정ID·시크릿 값)는 반드시 제거**하고 옮겨라.

---

## 5. 프로젝트 개요

- **프로젝트명**: VitaminS (Vit_S) — 그룹웨어 서비스, 3조
- **조직**: `wanted-VitS-Project` — ⚠️ 전 레포 **PUBLIC**
- **패키지**: `com.group3.vitamins` · `rootProject.name = vitamins`
- **Java**: 17 (toolchain) · **빌드**: Gradle (`gradlew`)
- **Spring Boot**: **3.5.16** (dependency-management 1.1.7) · Gradle 9.5.1
  > 2026-07-28 에 4.0.7 → 3.5.16 하향. 스타터 명칭이 3.x 계열이므로 Boot 4 문서를 참고하지 말 것.
- **주요 의존성**: Web(MVC) · WebFlux(WebClient 용) · Data JPA · MyBatis · Flyway · Security · Mail · **Session(Redis)** · Lombok · MySQL Connector · DevTools
  > ⚠️ **세션은 Redis 단일이다.** `spring-session-jdbc` 는 **의도적으로 넣지 않는다** — 2종이 동시에 있으면
  > 어느 저장소가 잡힐지 설정으로 정할 수 없다 (아래 §5 주의사항 참고). 추가하지 마라.

> 🔴 **알려진 이슈 — 테스트가 0개다**
> PR #4 에서 `VitaminSApplicationTests` 와 `src/test/resources/` 설정이 삭제되어
> **현재 테스트가 하나도 없다.** CI 의 `빌드 & 테스트` 는 항상 통과하지만 아무것도 검증하지 않으며,
> 테스트가 없는 동안 커버리지는 0 이다. 도메인 구현과 함께 테스트를 되살릴 것.
>
> ⚠️ **설정 파일 규칙**: 프로필 설정은 **`.yml` 확장자만** 쓴다 (`.yaml` 금지).
> 단 `.coderabbit.yaml` 은 CodeRabbit 이 `.yaml` 만 인식하므로 예외다.
> 세션 저장소는 **클래스패스로 자동 판별**된다. `spring.session.store-type` 은 Boot 3 에서 **삭제된 프로퍼티**라
> 적어도 무시된다 (2026-08-04 확인). 저장소를 하나로 못 박는 유일한 수단은 **`spring-session-jdbc` 를 넣지 않는 것**이다.
- **내 역할**: DevOps (인프라 · CI/CD · 빌드 표준화 · 문서 체계)
- **현재 마일스톤**: 파이널 모듈 — 초기 세팅 단계

### 연관 레포 (같은 조직)

| 레포 | 역할 |
|------|------|
| `Vit_S-GroupwareService-BackEnd` | 이 레포 — Spring Boot 백엔드 |
| `Vit_S-GroupwareService-FrontEnd` | 프론트엔드 |
| `Vit_S-GroupwareService-Python-Server` | 파이썬 서버 |

> ⚠️ **3개 레포 전부 DevOps 담당(김동현)이 관리한다.** 인프라·CI/CD 설계는 3개 서비스를 함께 놓고 하라.
> 이 레포에서 정한 컨벤션·워크플로우는 나머지 2개 레포에도 동일하게 적용하는 것을 기본으로 한다.

## 5-1. 명령어

| 목적 | 명령어 |
|------|--------|
| 빌드 | `./gradlew build` |
| 빌드 (테스트 제외) | `./gradlew build -x test` |
| 전체 테스트 | `./gradlew test` |
| 단일 테스트 | `./gradlew test --tests "com.group3.vitamins.클래스명"` |
| 로컬 실행 | `./gradlew bootRun` |
| 클린 빌드 | `./gradlew clean build` |

---

## 6. 보안 규칙 ⚠️ PUBLIC 레포

이 레포는 **누구나 볼 수 있다.** 아래를 커밋된 파일에 절대 쓰지 마라.

**금지 목록**
- 퍼블릭/프라이빗 IP, RDS·Redis 엔드포인트, 도메인
- AWS 계정 ID, ARN, 액세스 키, 키페어 이름
- DB 비밀번호, API 키, 토큰, 시크릿 **값**
- `.pem`, `.key`, `.env`, `application-local.yml` 등 자격 파일

**허용 / 권장**
- 시크릿은 **키 이름만** 기록 (`DB_PASSWORD`, `AWS_ACCESS_KEY_ID`)
- `.ai/INFRA.md` 에는 구조·역할·포트만 쓰고 실제 값은 `{{EC2_HOST}}` 형태로 마스킹
- 실제 값은 `.ai/local/INFRA-real.md` (gitignore)
- 런타임 주입은 GitHub Repo Secrets → 환경변수

**작업 중 민감 정보를 커밋하려는 흐름을 발견하면 즉시 중단하고 사용자에게 알려라.**

---

## 7. 양식

PR·Issue·커밋 메시지를 작성할 때는 해당 양식 파일을 **먼저 읽고** 그 형식에 맞춰라.

| 작업 | 읽을 파일 |
|------|----------|
| PR 작성 | `.ai/template/PR.md` |
| Issue 작성 | `.ai/template/ISSUE.md` |
| 커밋 메시지 | `.ai/template/COMMIT.md` |

---

## 8. 응답 스타일 (기본)

> 팀 공용 기준선이다. 개인 취향(친근체·이모지 강도 등)은 각자 `~/.claude/CLAUDE.md` 에 추가하라.

- **한국어**로 답한다.
- **결론 먼저, 설명 나중에.** 사용자가 빠르게 판단할 수 있게.
- 비교·정리·매핑은 **표**를 쓴다.
- 한 응답에 **4단계 이상 묶지 않는다.** 여러 단계 작업은 순서 표를 먼저 제시하고 하나씩 진행.
- 코드 제공 시 **경로를 항상 명시**하고, 코드 뒤에 각 부분의 역할을 짧게 설명한다.
- 선택지를 제시할 땐 **추천안을 먼저** 놓고 장단점을 나눈 뒤, 사용자 결정을 명시적으로 요청한다.
- 에러 대응은 `핵심 진단 한 줄 → 원인 → 해결 → 다음 행동` 순서.
- 매 응답 마지막에 **다음 액션**을 명확히 안내한다.

---

## 9. 절대 금지

- ❌ **선제적으로 커밋·푸시** (`git commit` / `git push`)
  → 사용자가 **"커밋해" · "푸시해" 라고 명시적으로 요청할 때만** 실행한다.
  요청이 없으면 양식대로 **메시지 텍스트만 제공**한다. (2026-08-03 완화)
- ❌ - PR 생성 (`gh pr create`) — 기본은 텍스트만 제공하되, 사용자가 "PR 올려" 등으로 **명시적으로 요청하면 실행 가능**.
- ❌ 필수 참조 문서(§3)를 읽지 않고 해당 영역 작업 시작ㅂ
- ❌ 문서에 없는 리소스 이름·엔드포인트를 추측해서 지어내기
- ❌ 민감 정보를 커밋되는 파일에 작성 (§6)
- ❌ `.ai/local/` 을 git에 추가
- ❌ `CLAUDE.md` 에 규칙 본문 작성 (포인터만 유지)
- ❌ STATE/WORKLOG 기록 빼먹기
- ❌ 코드만 던지고 설명 없이 끝내기
